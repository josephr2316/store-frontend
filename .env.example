# URL del API del backend.
# Desarrollo local (backend en tu máquina):
#   VITE_API_URL=http://localhost:8080
# Vercel: en el proyecto → Settings → Environment Variables añade (Build):
#   VITE_API_URL=https://store-production-3316.up.railway.app
# (o la URL real de tu backend en Railway/Render). Si no se define, el build usa la URL por defecto anterior.
